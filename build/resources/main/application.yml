server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/E-TicaretDB
    username: postgres
    password: asd123asd
    driverClassName: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  sql:
    init:
      platform: postgres